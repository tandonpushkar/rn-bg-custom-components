import {StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
  buttonStyle: {
    marginHorizontal: 8,
    borderRadius: 10,
    height: 50,
    width: 140,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textStyle: {textAlign: 'center', fontWeight: 'bold', fontSize: 16},
});
